name: CD
on:
  push:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: "178.128.57.37"
        uses: appleboy/ssh-action@master
        with:
          host: '178.128.57.37'
          username: ${{ secrets.USERNAME }}
          PASSWORD: ${{ secrets.PASSWORD }}
          port: 22
          script: |
            cd /home/magazine.afresto.id/public_html/
            git pull https://github.com/Fresto-Code/aristoteles-sim
            git reset --hard
